# Website assets

Add the final portrait as `profile.jpg` in this directory. The homepage and About page will use it automatically.

Recommended crop: shoulder-up, portrait orientation, approximately 4:5, using the original high-resolution image rather than a screenshot.
